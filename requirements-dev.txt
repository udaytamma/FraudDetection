# Fraud Detection Platform - Development Dependencies
# Install: pip install -r requirements.txt -r requirements-dev.txt
#
# These are NOT installed in the production Docker image. They exist only
# for local dev, CI test runs, and load testing. Keeping them out of the
# production image reduces Railway memory footprint and build time.

-r requirements.txt

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0

# Load Testing
locust>=2.24.0

# Development
black>=24.1.0
ruff>=0.1.0
mypy>=1.8.0

# Type stubs
types-PyYAML>=6.0.0
types-redis>=4.6.0
